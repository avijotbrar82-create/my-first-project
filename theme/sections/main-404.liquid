<section class="blk" style="padding:120px 0;text-align:center">
  <div class="container">
    <div class="eyebrow">Error 404</div>
    <h1 class="serif" style="font-size:clamp(38px,7vw,72px);margin:14px 0 0">This page has <em style="font-style:italic;color:var(--honey)">wandered off.</em></h1>
    <p style="color:var(--taupe);max-width:46ch;margin:20px auto 30px;line-height:1.6">The page you were looking for doesn't exist — but recovery is just a click away.</p>
    <a class="btn btn-honey" href="{{ routes.all_products_collection_url }}">Shop the range</a>
  </div>
</section>
{% schema %}
{ "name": "404", "settings": [] }
{% endschema %}
